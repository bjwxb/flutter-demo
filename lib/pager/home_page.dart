import 'package:flutter/material.dart';
import 'package:dio/dio.dart';

//首页
class HomePage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("home pager"),
      ),
    );
  }

}