import 'package:flutter/material.dart';

//健康
class HealthPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("health pager"),
      ),
    );
  }

}